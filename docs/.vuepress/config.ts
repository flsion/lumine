import {defaultTheme, defineUserConfig} from 'vuepress'

export default defineUserConfig({
    lang: 'zh-CN',
    title: 'Lumine',
    description: '学习、分享、结识、交流',
    base: '/lumine/',
    theme: defaultTheme({})
})